
//usar next link y next image
//uso de use memo para filtrar cuando agregue los nuevos elementos
//paginar resultados
//responsive
//testing

Poner en el readme:

DOne:
//agregar tema
//agregar layout
//cambiar common components y components
//imports relativos
//agregar 404
//Agregar loader
Explicar:
//usar react hook form y schemas
//usar axios o rquery
//poner en barrel file
const filteredProducts = useMemo(() => {
if (selectedCategory) {
return products.filter(
(product) => product.category === selectedCategory
);
}
return products;
}, [products, selectedCategory]);
